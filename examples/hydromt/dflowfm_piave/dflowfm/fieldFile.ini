# written by HYDROLIB-core 0.5.2

[General]
fileVersion = 2.00     # File version. Do not edit this.
fileType    = iniField # File type. Should be 'iniField'. Do not edit this.

[Initial]
quantity            = bedlevel             # Name of the quantity. See UM Table D.2.
dataFile            = ../maps/bedlevel.tif # Name of file containing field data values.
dataFileType        = GeoTIFF              # Type of dataFile.
interpolationMethod = averaging            # Type of (spatial) interpolation.
operand             = O                    # How this data is combined with previous data for the same quantity (if any).
averagingType       = nearestNb            # Type of averaging, if interpolationMethod=averaging .
averagingRelSize    = 0.37                 # Relative search cell size for averaging.
averagingNumMin     = 1                    # Minimum number of points in averaging. Must be ≥ 1.
averagingPercentile = 0                    # Percentile value for which data values to include in averaging. 0.0 means off.
extrapolationMethod = 0                    # Option for (spatial) extrapolation.
locationType        = 2d                   # Target location of interpolation.

[Initial]
quantity            = InfiltrationCapacity             # Name of the quantity. See UM Table D.2.
dataFile            = ../maps/InfiltrationCapacity.tif # Name of file containing field data values.
dataFileType        = GeoTIFF                          # Type of dataFile.
interpolationMethod = triangulation                    # Type of (spatial) interpolation.
operand             = O                                # How this data is combined with previous data for the same quantity (if any).
averagingType       = mean                             # Type of averaging, if interpolationMethod=averaging .
averagingRelSize    = 1.01                             # Relative search cell size for averaging.
averagingNumMin     = 1                                # Minimum number of points in averaging. Must be ≥ 1.
averagingPercentile = 0                                # Percentile value for which data values to include in averaging. 0.0 means off.
extrapolationMethod = 0                                # Option for (spatial) extrapolation.
locationType        = 2d                               # Target location of interpolation.

[Parameter]
quantity            = frictioncoefficient             # Name of the quantity. See UM Table D.2.
dataFile            = ../maps/frictioncoefficient.tif # Name of file containing field data values.
dataFileType        = GeoTIFF                         # Type of dataFile.
interpolationMethod = triangulation                   # Type of (spatial) interpolation.
operand             = O                               # How this data is combined with previous data for the same quantity (if any).
averagingType       = mean                            # Type of averaging, if interpolationMethod=averaging .
averagingRelSize    = 1.01                            # Relative search cell size for averaging.
averagingNumMin     = 1                               # Minimum number of points in averaging. Must be ≥ 1.
averagingPercentile = 0                               # Percentile value for which data values to include in averaging. 0.0 means off.
extrapolationMethod = 0                               # Option for (spatial) extrapolation.
locationType        = 2d                              # Target location of interpolation.

